let message; //used to be var, has a few differences, but let took over
message = "Hello";

alert(message);

/*
never hyphen a name
can use $ or _ in names
case sensitive
no using reserved words like 'return' and 'let'+
*/
